apiVersion: v1
kind: Service
metadata:
  name: {{ include "clisonix.fullname" . }}
  labels:
    {{- include "clisonix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clisonix.selectorLabels" . | nindent 4 }}
